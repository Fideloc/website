---
import { stegaClean } from "@sanity/client/stega";
import CategoryCard from "./CategoryCard.astro";
import ArrowLink from "./ArrowLink.astro";

interface Props {
  sectionTitle?: string | null;
  viewAllLabel?: string | null;
  categories: Array<{
    name: string;
    slug: string;
    description?: string | null;
    imageUrl: string | null;
  }>;
}

const { sectionTitle, viewAllLabel, categories } = Astro.props;

const cleanSectionTitle = stegaClean(sectionTitle ?? "");
const cleanViewAllLabel = stegaClean(viewAllLabel ?? "");
---

<section class="mx-auto max-w-7xl px-4 py-14 md:px-8">
  <div class="mb-8 flex items-baseline justify-between">
    <h2 class="font-heading text-primary text-2xl font-semibold md:text-3xl">
      {cleanSectionTitle}
    </h2>
    {
      cleanViewAllLabel && (
        <ArrowLink href="/produits">{cleanViewAllLabel}</ArrowLink>
      )
    }
  </div>

  <div class="grid grid-cols-2 gap-6 lg:grid-cols-4">
    {categories.slice(0, 4).map((cat) => <CategoryCard {...cat} />)}
  </div>
</section>
